🧠 wget Cheat Sheet

🔍 Basic Usage
wget example.com                     # Download file
wget -O custom.html example.com      # Save with custom name
wget -P /path/dir/ url               # Download to directory
wget -i urls.txt                     # Download from list of URLs
wget --spider url                    # Check if file exists

📥 Download Options
wget -c url                          # Continue/resume download
wget -b url                          # Download in background
wget --limit-rate=200k url           # Limit download speed
wget -q url                          # Quiet mode
wget -nv url                         # Non-verbose (no progress)
wget --show-progress url             # Show only progress bar

🔄 Recursive Downloads
wget -r url                          # Recursive download
wget -r -l 3 url                     # Limit recursion depth to 3
wget -m url                          # Mirror entire site
wget -np -r url                      # No parent directories
wget -r -A pdf url                   # Download only PDFs
wget -r -R jpg,png url               # Reject jpg and png files

🌐 Website Mirroring
wget -m -k -p url                    # Mirror with converted links
wget -m -np -k -p url                # Mirror without parent dirs
wget --mirror --convert-links --page-requisites url
wget -r -l inf -k -p url             # Complete site download

🔐 Authentication
wget --user=username --password=pass url     # HTTP auth
wget --ask-password url                      # Prompt for password
wget --http-user=user --http-password=pass url
wget --no-check-certificate url              # Ignore SSL errors
wget --certificate=cert.pem url              # Client certificate

🏷️ Headers and User Agent
wget --header="Name: value" url      # Custom header
wget --user-agent="Agent" url        # Custom user agent
wget -U "Mozilla/5.0" url            # Set user agent
wget --referer=url url               # Set referer
wget --header="Accept: text/html" url

🍪 Cookies
wget --load-cookies cookies.txt url  # Load cookies from file
wget --save-cookies cookies.txt url  # Save cookies to file
wget --keep-session-cookies url      # Keep session cookies
wget --no-cookies url                # Ignore cookies

⏱️ Timeouts and Retries
wget -t 5 url                        # Retry 5 times
wget -t 0 url                        # Retry infinitely
wget -T 30 url                       # Timeout 30 seconds
wget --wait=2 url                    # Wait 2s between requests
wget --random-wait url               # Random wait between requests
wget --waitretry=10 url              # Wait 10s before retry

🔗 FTP Downloads
wget ftp://server/file               # Download from FTP
wget --ftp-user=user --ftp-password=pass ftp://server/file
wget -r ftp://server/dir/            # Recursive FTP download
wget --no-passive-ftp ftp://url      # Use active FTP

📋 Logging and Output
wget -o log.txt url                  # Write log to file
wget -a log.txt url                  # Append to log file
wget -d url                          # Debug output
wget -v url                          # Verbose output
wget --no-verbose url                # Minimal output

🔧 Advanced Options
wget -c -t 0 --timeout=30 url        # Robust download
wget --proxy=on --proxy-user=user url # Use proxy
wget --no-proxy url                  # Bypass proxy
wget -N url                          # Timestamping (only newer)
wget -k url                          # Convert links for local viewing
wget --delete-after url              # Delete after download

🧪 Testing and Bandwidth
wget --spider --server-response url  # Check headers only
wget -S url                          # Show server response
wget --quota=10m -r url              # Stop after 10MB
wget --reject-regex pattern url      # Reject URLs matching pattern
wget --accept-regex pattern url      # Accept URLs matching pattern

✅ Done. Happy downloading!
