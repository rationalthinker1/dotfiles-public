.vim/bundle
.vim/plugged
.ssh.bak
.ssh/known_hosts
oh-my-zsh
tmp
.vim/undo/*
!.vim/undo/.gitkeep
backup
config/zsh/antigen
config/zsh/local.zsh
config/zsh/.zsh_history
fish/local.fish
fish/fish_variables
.zcompdump
.zcompdump-*
.vim/.viminfo
fonts/.installed
config/zsh/.zshrc.zwc
.vim/pack/kite
tags
.vim/spell/
init.vim.bak
config/zsh/.zcompdump*
*.bak
config/zsh/.zplug
config/.aws/largefile
config/.aws/credentials
config/.aws/cli/history
config/.aws/cli/cache
*.DS_Store
*:Zone.Identifiere
*.zwc
.vscode/*
config/zsh/cache/*
*.backup
*.old
.vim/tmp/
*.log
.gitconfig
# --- public-only excludes (sanitized mirror of master) ---
# The config/ variants are where these live on master since the config/ restructure; the
# bare root forms are kept as well so a stray file at either location stays excluded.
.ssh/
.aws/
config/.aws/
password-store/
config/password-store/
windows-terminal/settings.json
zsh/references/aws.md
config/zsh/references/aws.md
zsh/local.zsh
config/zsh/local.zsh
.gitconfig
# Root CLAUDE.md only — it documents the private remote and the sensitive-path
# list. Anchored with a leading slash so claude/CLAUDE.md (generic, safe) still syncs.
/CLAUDE.md
# graft's generated graph/cards — untracked here, ignored to keep status clean.
graft/
